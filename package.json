{
  "name": "autosell",
  "version": "1.0.0",
  "description": "WA Bulk Sender com IA Anti-Ban — Evolution API + NVIDIA",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18.x"
  }
}
